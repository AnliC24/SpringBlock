spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: tern-config
      profile: dev
  application:
    name: tern-blockchain-order
eureka:
  client:
    service-url:
      defaultZone: http://106.14.211.161:8771/eureka/
server:
  port: 8776

