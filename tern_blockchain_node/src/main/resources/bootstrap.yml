spring:
  application:
    name: tern-blockchain-node
  cloud:
    config:
      discovery:
        enabled: true
        service-id: tern-config
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://106.14.211.161:8771/eureka/
